openapi: 3.0.3
info:
  title: NetTechPro API - Complete Documentation
  description: |
    ## 🌐 REST API cho Website Bán Thiết Bị Mạng NetTechPro
    
    ### 📚 API Categories:
    - **Public API** - Cho Client (không cần auth)
    - **User API** - Cho khách hàng đã đăng nhập
    - **Admin API** - Cho quản trị viên (cần admin role)
    
    ### 🔐 Authentication:
    Bearer token trong header:
    ```
    Authorization: Bearer <your_token>
    ```
    
    ### 📝 Response Format:
    ```json
    {
      "success": true,
      "data": {...},
      "message": "Success message"
    }
    ```
    
    ### 🚀 Base URL:
    - Development: http://localhost:3000
    - Production: https://api.nettechpro.com
  version: 2.0.0
  contact:
    name: NetTechPro API Support
    email: api@nettechpro.com
  license:
    name: MIT
    url: https://opensource.org/licenses/MIT

servers:
  - url: http://localhost:3000
    description: Development server
  - url: https://api.nettechpro.com
    description: Production server

tags:
  - name: System
    description: 🔧 Hệ thống
  - name: Auth
    description: 🔐 Xác thực & Đăng nhập
  - name: Public - Products
    description: 🛍️ Sản phẩm (Public)
  - name: Public - Categories
    description: 📂 Danh mục (Public)
  - name: Public - Cart
    description: 🛒 Giỏ hàng
  - name: User - Orders
    description: 📦 Đơn hàng của khách (User)
  - name: Admin - Products
    description: 📦 Quản lý sản phẩm (Admin)
  - name: Admin - Orders
    description: 📋 Quản lý đơn hàng (Admin)
  - name: Admin - Customers
    description: 👥 Quản lý khách hàng (Admin)
  - name: Admin - Categories
    description: 🗂️ Quản lý danh mục (Admin)
  - name: Admin - Dashboard
    description: 📊 Dashboard & Analytics (Admin)

paths:
  # ===== SYSTEM =====
  /api/health:
    get:
      summary: Health check
      description: Kiểm tra trạng thái server
      tags: [System]
      responses:
        '200':
          description: Server hoạt động bình thường
          content:
            application/json:
              schema:
                type: object
                properties:
                  ok:
                    type: boolean
                    example: true
                  timestamp:
                    type: string
                    format: date-time
                    example: "2025-01-08T10:30:00Z"
                  version:
                    type: string
                    example: "2.0.0"

  # ===== AUTH =====
  /api/auth/register:
    post:
      summary: Đăng ký tài khoản
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password, name]
              properties:
                email:
                  type: string
                  format: email
                  example: "user@example.com"
                password:
                  type: string
                  format: password
                  example: "Password123!"
                name:
                  type: string
                  example: "Nguyễn Văn A"
                phone:
                  type: string
                  example: "0901234567"
      responses:
        '201':
          description: Đăng ký thành công
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: object
                    properties:
                      user:
                        $ref: '#/components/schemas/User'
                      token:
                        type: string
                        example: "eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9..."
        '400':
          description: Email đã tồn tại

  /api/auth/login:
    post:
      summary: Đăng nhập
      tags: [Auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  example: "user@example.com"
                password:
                  type: string
                  example: "Password123!"
      responses:
        '200':
          description: Đăng nhập thành công
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      user:
                        $ref: '#/components/schemas/User'
                      token:
                        type: string

  /api/auth/me:
    get:
      summary: Lấy thông tin user hiện tại
      tags: [Auth]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Thông tin user
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/User'

  # ===== PUBLIC - PRODUCTS =====
  /api/products:
    get:
      summary: Danh sách sản phẩm (Public)
      description: Lấy danh sách sản phẩm cho website client
      tags: [Public - Products]
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 12
        - name: category
          in: query
          schema:
            type: string
          description: Lọc theo danh mục
        - name: brand
          in: query
          schema:
            type: string
          description: Lọc theo thương hiệu
        - name: minPrice
          in: query
          schema:
            type: number
        - name: maxPrice
          in: query
          schema:
            type: number
        - name: search
          in: query
          schema:
            type: string
          description: Tìm kiếm theo tên
        - name: sort
          in: query
          schema:
            type: string
            enum: [newest, price-asc, price-desc, popular]
            default: newest
      responses:
        '200':
          description: Danh sách sản phẩm
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Product'
                  pagination:
                    $ref: '#/components/schemas/Pagination'

  /api/products/{id}:
    get:
      summary: Chi tiết sản phẩm
      tags: [Public - Products]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Chi tiết sản phẩm
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/ProductDetail'
        '404':
          description: Không tìm thấy sản phẩm

  /api/products/featured:
    get:
      summary: Sản phẩm nổi bật
      tags: [Public - Products]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 8
      responses:
        '200':
          description: Danh sách sản phẩm nổi bật
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Product'

  /api/products/bestsellers:
    get:
      summary: Sản phẩm bán chạy
      tags: [Public - Products]
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 8
      responses:
        '200':
          description: Danh sách sản phẩm bán chạy

  # ===== PUBLIC - CATEGORIES =====
  /api/categories:
    get:
      summary: Danh sách danh mục
      tags: [Public - Categories]
      responses:
        '200':
          description: Danh sách danh mục
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Category'

  /api/categories/{slug}:
    get:
      summary: Chi tiết danh mục
      tags: [Public - Categories]
      parameters:
        - name: slug
          in: path
          required: true
          schema:
            type: string
          example: router
      responses:
        '200':
          description: Chi tiết danh mục
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/CategoryDetail'

  # ===== USER - ORDERS =====
  /api/orders:
    post:
      summary: Tạo đơn hàng mới
      tags: [User - Orders]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [items, shippingAddress, paymentMethod]
              properties:
                items:
                  type: array
                  items:
                    type: object
                    properties:
                      productId:
                        type: string
                      quantity:
                        type: integer
                shippingAddress:
                  $ref: '#/components/schemas/Address'
                paymentMethod:
                  type: string
                  enum: [COD, VNPay, MoMo, ZaloPay]
                note:
                  type: string
      responses:
        '201':
          description: Tạo đơn hàng thành công
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/OrderDetail'

    get:
      summary: Lịch sử đơn hàng của user
      tags: [User - Orders]
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: status
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Danh sách đơn hàng
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Order'

  /api/orders/{id}:
    get:
      summary: Chi tiết đơn hàng
      tags: [User - Orders]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Chi tiết đơn hàng
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/OrderDetail'

  /api/orders/{id}/cancel:
    post:
      summary: Hủy đơn hàng
      tags: [User - Orders]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        content:
          application/json:
            schema:
              type: object
              properties:
                reason:
                  type: string
      responses:
        '200':
          description: Hủy đơn thành công

  # ===== ADMIN - PRODUCTS =====
  /api/admin/products:
    get:
      summary: Danh sách sản phẩm (Admin)
      description: Lấy danh sách tất cả sản phẩm với phân trang
      tags: [Admin - Products]
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
        - name: search
          in: query
          schema:
            type: string
          description: Tìm kiếm theo tên
        - name: category
          in: query
          schema:
            type: string
          description: Lọc theo danh mục
        - name: status
          in: query
          schema:
            type: string
            enum: [active, inactive, draft]
      responses:
        '200':
          description: Danh sách sản phẩm
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                    example: true
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/AdminProduct'
                  pagination:
                    $ref: '#/components/schemas/Pagination'

    post:
      summary: Tạo sản phẩm mới
      tags: [Admin - Products]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductInput'
      responses:
        '201':
          description: Tạo thành công
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/AdminProduct'

  /api/admin/products/{id}:
    get:
      summary: Chi tiết sản phẩm (Admin)
      tags: [Admin - Products]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Chi tiết sản phẩm
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/AdminProduct'

    put:
      summary: Cập nhật sản phẩm
      tags: [Admin - Products]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/ProductInput'
      responses:
        '200':
          description: Cập nhật thành công

    delete:
      summary: Xóa sản phẩm
      tags: [Admin - Products]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Xóa thành công

  /api/admin/products/{id}/toggle-status:
    patch:
      summary: Bật/tắt sản phẩm
      tags: [Admin - Products]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Cập nhật trạng thái thành công

  # ===== ADMIN - ORDERS =====
  /api/admin/orders:
    get:
      summary: Danh sách đơn hàng (Admin)
      tags: [Admin - Orders]
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
        - name: status
          in: query
          schema:
            type: string
            enum: [pending, processing, shipping, completed, cancelled]
        - name: paymentMethod
          in: query
          schema:
            type: string
        - name: search
          in: query
          schema:
            type: string
          description: Tìm theo mã đơn hoặc tên khách
      responses:
        '200':
          description: Danh sách đơn hàng
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Order'
                  pagination:
                    $ref: '#/components/schemas/Pagination'

  /api/admin/orders/{id}:
    get:
      summary: Chi tiết đơn hàng (Admin)
      tags: [Admin - Orders]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Chi tiết đơn hàng
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/OrderDetail'

  /api/admin/orders/{id}/status:
    put:
      summary: Cập nhật trạng thái đơn hàng
      tags: [Admin - Orders]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [status]
              properties:
                status:
                  type: string
                  enum: [pending, processing, shipping, completed, cancelled]
                note:
                  type: string
      responses:
        '200':
          description: Cập nhật thành công

  # ===== ADMIN - CUSTOMERS =====
  /api/admin/customers:
    get:
      summary: Danh sách khách hàng
      tags: [Admin - Customers]
      security:
        - bearerAuth: []
      parameters:
        - name: page
          in: query
          schema:
            type: integer
            default: 1
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
        - name: search
          in: query
          schema:
            type: string
      responses:
        '200':
          description: Danh sách khách hàng
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/Customer'
                  pagination:
                    $ref: '#/components/schemas/Pagination'

  /api/admin/customers/{id}:
    get:
      summary: Chi tiết khách hàng
      tags: [Admin - Customers]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Chi tiết khách hàng
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    $ref: '#/components/schemas/CustomerDetail'

  # ===== ADMIN - CATEGORIES =====
  /api/admin/categories:
    get:
      summary: Danh sách danh mục (Admin)
      tags: [Admin - Categories]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Danh sách danh mục
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: array
                    items:
                      $ref: '#/components/schemas/CategoryDetail'

    post:
      summary: Tạo danh mục mới
      tags: [Admin - Categories]
      security:
        - bearerAuth: []
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoryInput'
      responses:
        '201':
          description: Tạo thành công

  /api/admin/categories/{id}:
    put:
      summary: Cập nhật danh mục
      tags: [Admin - Categories]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      requestBody:
        required: true
        content:
          application/json:
            schema:
              $ref: '#/components/schemas/CategoryInput'
      responses:
        '200':
          description: Cập nhật thành công

    delete:
      summary: Xóa danh mục
      tags: [Admin - Categories]
      security:
        - bearerAuth: []
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
      responses:
        '200':
          description: Xóa thành công

  # ===== ADMIN - DASHBOARD =====
  /api/admin/dashboard/metrics:
    get:
      summary: Dashboard metrics
      description: Lấy các metrics tổng quan cho dashboard
      tags: [Admin - Dashboard]
      security:
        - bearerAuth: []
      responses:
        '200':
          description: Dashboard metrics
          content:
            application/json:
              schema:
                type: object
                properties:
                  success:
                    type: boolean
                  data:
                    type: object
                    properties:
                      totalRevenue:
                        type: number
                        example: 125000000
                        description: Tổng doanh thu (VNĐ)
                      totalOrders:
                        type: integer
                        example: 245
                        description: Tổng số đơn hàng
                      totalProducts:
                        type: integer
                        example: 156
                        description: Tổng số sản phẩm
                      totalCustomers:
                        type: integer
                        example: 892
                        description: Tổng số khách hàng
                      revenueGrowth:
                        type: number
                        example: 12.5
                        description: % tăng trưởng doanh thu
                      ordersGrowth:
                        type: number
                        example: 8.3
                        description: % tăng trưởng đơn hàng

  /api/admin/dashboard/revenue-chart:
    get:
      summary: Biểu đồ doanh thu
      tags: [Admin - Dashboard]
      security:
        - bearerAuth: []
      parameters:
        - name: period
          in: query
          schema:
            type: string
            enum: [week, month, year]
            default: month
      responses:
        '200':
          description: Data cho biểu đồ doanh thu

  /api/admin/dashboard/top-products:
    get:
      summary: Top sản phẩm bán chạy
      tags: [Admin - Dashboard]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 5
      responses:
        '200':
          description: Danh sách top sản phẩm

  /api/admin/dashboard/recent-orders:
    get:
      summary: Đơn hàng gần đây
      tags: [Admin - Dashboard]
      security:
        - bearerAuth: []
      parameters:
        - name: limit
          in: query
          schema:
            type: integer
            default: 10
      responses:
        '200':
          description: Danh sách đơn hàng gần đây

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      bearerFormat: JWT
      description: |
        JWT Bearer token
        
        **Cách lấy token:**
        1. Đăng nhập qua `/api/auth/login`
        2. Copy token từ response
        3. Thêm vào header: `Authorization: Bearer <token>`

  schemas:
    # ===== USER =====
    User:
      type: object
      properties:
        id:
          type: string
          example: "user_123"
        email:
          type: string
          example: "user@example.com"
        name:
          type: string
          example: "Nguyễn Văn A"
        phone:
          type: string
          example: "0901234567"
        role:
          type: string
          enum: [user, admin]
          example: "user"
        createdAt:
          type: string
          format: date-time

    # ===== PRODUCT =====
    Product:
      type: object
      properties:
        id:
          type: string
          example: "1"
        title:
          type: string
          example: "TP-Link Archer C6 - Router WiFi AC1200"
        price:
          type: number
          example: 850000
        discountedPrice:
          type: number
          example: 650000
        reviews:
          type: integer
          example: 128
        imgs:
          type: object
          properties:
            thumbnails:
              type: array
              items:
                type: string
            previews:
              type: array
              items:
                type: string
        category:
          type: string
          example: "Router"
        brand:
          type: string
          example: "TP-Link"
        stock:
          type: integer
          example: 85

    ProductDetail:
      allOf:
        - $ref: '#/components/schemas/Product'
        - type: object
          properties:
            description:
              type: string
            shortDescription:
              type: string
            specifications:
              type: object
              example:
                cpu: "Dual-core 800MHz"
                ram: "128MB"
                ports: "4 x LAN 10/100Mbps, 1 x WAN"
            relatedProducts:
              type: array
              items:
                $ref: '#/components/schemas/Product'

    AdminProduct:
      allOf:
        - $ref: '#/components/schemas/Product'
        - type: object
          properties:
            sku:
              type: string
              example: "TP-C6-001"
            sold:
              type: integer
              example: 189
            status:
              type: string
              enum: [active, inactive, draft]
              example: "active"
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time

    ProductInput:
      type: object
      required: [name, category, brand, price, stock]
      properties:
        name:
          type: string
          example: "TP-Link Archer C6"
        sku:
          type: string
          example: "TP-C6-001"
        category:
          type: string
          example: "Router"
        brand:
          type: string
          example: "TP-Link"
        price:
          type: number
          example: 850000
        salePrice:
          type: number
          example: 650000
        stock:
          type: integer
          example: 100
        description:
          type: string
        shortDescription:
          type: string
        specifications:
          type: object
        images:
          type: array
          items:
            type: string
        tags:
          type: array
          items:
            type: string

    # ===== ORDER =====
    Order:
      type: object
      properties:
        id:
          type: string
          example: "ORD-20250108-001"
        customerName:
          type: string
          example: "Nguyễn Văn A"
        customerEmail:
          type: string
          example: "user@example.com"
        items:
          type: integer
          example: 3
        total:
          type: number
          example: 2150000
        status:
          type: string
          enum: [pending, processing, shipping, completed, cancelled]
          example: "processing"
        paymentMethod:
          type: string
          enum: [COD, VNPay, MoMo, ZaloPay]
          example: "VNPay"
        date:
          type: string
          format: date-time

    OrderDetail:
      allOf:
        - $ref: '#/components/schemas/Order'
        - type: object
          properties:
            customerInfo:
              type: object
              properties:
                name:
                  type: string
                email:
                  type: string
                phone:
                  type: string
            items:
              type: array
              items:
                type: object
                properties:
                  productId:
                    type: string
                  productName:
                    type: string
                  quantity:
                    type: integer
                  price:
                    type: number
                  total:
                    type: number
            shippingAddress:
              $ref: '#/components/schemas/Address'
            subtotal:
              type: number
            shippingFee:
              type: number
            discount:
              type: number
            total:
              type: number
            timeline:
              type: array
              items:
                type: object
                properties:
                  status:
                    type: string
                  timestamp:
                    type: string
                    format: date-time
                  note:
                    type: string

    # ===== CUSTOMER =====
    Customer:
      type: object
      properties:
        id:
          type: string
          example: "cust_123"
        name:
          type: string
          example: "Nguyễn Văn A"
        email:
          type: string
          example: "customer@example.com"
        phone:
          type: string
          example: "0901234567"
        totalOrders:
          type: integer
          example: 12
        totalSpent:
          type: number
          example: 15000000
        lastOrder:
          type: string
          format: date-time
        status:
          type: string
          enum: [active, inactive]

    CustomerDetail:
      allOf:
        - $ref: '#/components/schemas/Customer'
        - type: object
          properties:
            orders:
              type: array
              items:
                $ref: '#/components/schemas/Order'
            addresses:
              type: array
              items:
                $ref: '#/components/schemas/Address'

    # ===== CATEGORY =====
    Category:
      type: object
      properties:
        id:
          type: string
        name:
          type: string
          example: "Router"
        slug:
          type: string
          example: "router"
        icon:
          type: string
        productCount:
          type: integer

    CategoryDetail:
      allOf:
        - $ref: '#/components/schemas/Category'
        - type: object
          properties:
            description:
              type: string
            image:
              type: string
            status:
              type: string
              enum: [active, inactive]

    CategoryInput:
      type: object
      required: [name]
      properties:
        name:
          type: string
        slug:
          type: string
        description:
          type: string
        icon:
          type: string
        image:
          type: string

    # ===== OTHER =====
    Address:
      type: object
      properties:
        fullName:
          type: string
        phone:
          type: string
        street:
          type: string
        ward:
          type: string
        district:
          type: string
        city:
          type: string
        zipCode:
          type: string

    Pagination:
      type: object
      properties:
        page:
          type: integer
          example: 1
        limit:
          type: integer
          example: 10
        total:
          type: integer
          example: 156
        totalPages:
          type: integer
          example: 16
        hasNext:
          type: boolean
          example: true
        hasPrev:
          type: boolean
          example: false

    Error:
      type: object
      properties:
        success:
          type: boolean
          example: false
        message:
          type: string
          example: "Error message"
        errors:
          type: array
          items:
            type: string
